import { CustomUrlSerializer } from './custom-url-serializer';

describe('CustomUrlSerializer', () => {
  it('should create an instance', () => {
    expect(new CustomUrlSerializer()).toBeTruthy();
  });
});
